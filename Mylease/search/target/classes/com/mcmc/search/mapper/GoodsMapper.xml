<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mcmc.search.mapper.GoodsMapper" >
	
	<select id="getGoodsList" resultType="com.mcmc.search.pojo.Goods">
		SELECT
			goods.id,
			goods.goods_name,
			goods.goods_info,
			goods.goods_deal_type,
			goods.goods_type,
			goods.goods_price,
			goods.goods_img
		FROM
			goods
		WHERE
			goods.can_show = 1

	</select>

</mapper>